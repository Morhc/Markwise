{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Markwise window. Dialogs and file I/O run on the Rust side, so only core defaults are exposed to the webview; the app's own commands (bridge_message, get_markdown_result) are always callable.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
