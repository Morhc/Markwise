{
  "name": "markwise",
  "version": "0.1.0",
  "private": true,
  "description": "Typora-style WYSIWYG markdown editor bundled for a native WebView2 app",
  "type": "module",
  "scripts": {
    "bundle": "node build.mjs",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "dependencies": {
    "@milkdown/crepe": "^7.6.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "esbuild": "^0.24.0"
  }
}
